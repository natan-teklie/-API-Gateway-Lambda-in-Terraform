# -API-Gateway-Lambda-in-Terraform
I create an API Gateway and a Lambda in Terraform. So that The API can receive a request from a client so that it invokes the lambda function in order to get a response.....
